logging:
  level:
    cn.codemao.codemaster.order: debug
spring:
  profiles:
    active: test
  datasource:
    name: order_test
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  redis:
    host: localhost
    port: 16001
  flyway:
    enabled: true

mariaDB4j:
    port: 3307
    dataDir:
